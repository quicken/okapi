import * as React from "react";
interface LayoutProp {
    children?: React.ReactNode;
}
export declare class Layout extends React.Component<LayoutProp> {
    render(): JSX.Element;
}
export default Layout;
